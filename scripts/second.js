// Variables
import * as p5 from "../scripts/p5js/p5.min.js";

const cRed = num.toString(16);
console.log(cRed);
// Setup
function setup() {
  createCanvas(windowWidth - 100, windowHeight - 100);
  frameRate(30);
  textSize(32);
}

// Draw
function draw() {
  text();
  //   bgCol = map(mouseX, 0, 1000, 0, 255);
  //   background(bgCol);
  //   fill(155);
  //   ellipse(mouseX, mouseY, 64, 64);
}
// Functions
